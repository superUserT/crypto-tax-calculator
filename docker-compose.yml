version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:80"  # Map localhost:8000 to container:80
    volumes:
      # Sync local folder to container so you can edit code without rebuilding
      - ./backend:/var/www/html
    env_file:
      - ./backend/.env
